# Copyright (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

[build-system]
requires = ["setuptools>=78.1.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inference-router"
version = "2026.2.0"
description = "A pluggable inference router for chat completion requests with local vLLM support"
readme = "README.md"
requires-python = ">=3.10,<3.14"
license = { text = "Apache-2.0" }
authors = []
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "fastapi>=0.104.0",
    "litellm==1.86.1",
    "uvicorn[standard]>=0.24.0",
    "pydantic>=2.0.0",
    "pyyaml>=6.0.0",
    "requests>=2.31.0",
    "tiktoken>=0.8.0",
    "gunicorn>=21.2.0",
    # Security pins (Trivy HIGH):
    #   setuptools<78.1.1 -> CVE-2025-47273 (PackageIndex path traversal)
    #   msgpack<1.2.1     -> GHSA-6v7p-g79w-8964 (OOB read on Unpacker reuse)
    # msgpack is a transitive dep; both are pinned here so pip upgrades them in
    # the runtime image.
    "setuptools>=78.1.1",
    "msgpack>=1.2.1",
]

[project.optional-dependencies]
ov = [
    "openvino>=2026.2",
    "optimum-intel[openvino]==2.0.0",
    "torch==2.13.0"
]

dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.5.0",
    "isort>=5.12.0",
    "openai>=1.0.0",
]

[tool.setuptools.packages.find]
include = ["src*"]
namespaces = false

[tool.setuptools.package-data]
"src.rsd" = ["*.yaml"]
"src.rsd.tools" = ["prompts/*.txt"]

[tool.black]
line-length = 100
target-version = ["py39", "py310", "py311"]

[tool.ruff]
line-length = 100
target-version = "py39"
select = ["E", "F", "W", "I"]

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--strict-markers -v"
markers = [
    "unit: unit tests",
    "integration: integration tests",
    "slow: slow tests",
]
